extends layout

block content
  script.
    window.fbAsyncInit = function () {
      FB.init({
        appId: "1806054702946735",
        xfbml: true,
        version: "v2.6"
      });

    };

    (function (d, s, id) {
      var js, fjs = d.getElementsByTagName(s)[0];
      if (d.getElementById(id)) {
        return;
      }
      js = d.createElement(s);
      js.id = id;
      js.src = "//connect.facebook.net/cs_CZ/sdk.js";
      fjs.parentNode.insertBefore(js, fjs);
    }(document, 'script', 'facebook-jssdk'));

  h1= "My shopping cart"
  div Facebook enables only localization (localize only "Not you?" message if you're logged in), white color and
  different button sizes. We are in discussion with Facebook to allow complete custom design.
  br
  br
  div(class="fb-send-to-messenger", messenger_app_id="1806054702946735", page_id=facebookPageId, color="white",
  size="standard", data-ref="SOME_ENCODED_DATA")
